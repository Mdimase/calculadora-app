import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.unnoba.calculadoraApp',
  appName: 'calculadora-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
